{% extends 'base.html.twig' %}

{% block title %}Maxime et Claire{% endblock %}

{% block body %}
    {% if app.user != null %}
        <donationpromiseform :present = "{{present}}" user-roles="{{ app.user.getRolesString() }}"/>
    {% else %}
        <donationpromiseform :present = "{{present}}" user-roles=""/>
    {% endif %}
{% endblock %}